<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.putao.wd"
    android:versionCode="1"
    android:versionName="1.0.0" >

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.GET_TASKS" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.MANAGE_ACCOUNTS" />
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.KILL_BACKGROUND_PROCESSES" />
    <uses-permission android:name="android.permission.CAMERA" />

    <uses-feature android:name="android.hardware.camera" />
    <uses-feature android:name="android.hardware.camera.autofocus" />

    <application
        android:name=".GlobalApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme" >

        <!-- 分属app_id -->
        <meta-data
            android:name="app_id"
            android:value="" />
        <!-- 主页 -->
        <activity
            android:name=".MainActivity"
            android:launchMode="singleTask" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!-- 登录注册 -->
        <activity
            android:name=".user.LoginActivity"
            android:screenOrientation="portrait" />
        <!-- 收货地址列表 -->
        <activity
            android:name=".me.address.AddressListActivity"
            android:screenOrientation="portrait" />
        <!-- 收货地址编辑 -->
        <activity
            android:name=".me.address.AddressEditActivity"
            android:screenOrientation="portrait" />
        <!-- 商品详情 -->
        <activity
            android:name=".product.ProductDetailActivity"
            android:screenOrientation="portrait" />
        <!-- 关于我们 -->
        <activity
            android:name=".me.address.AboutUsActivity"
            android:screenOrientation="portrait" />
        <!-- 设置 -->
        <activity
            android:name=".me.setting.SettingActivity"
            android:screenOrientation="portrait" />
        <!-- 消息中心 -->
        <activity
            android:name=".me.message.MessageCenterActivity"
            android:screenOrientation="portrait" />
        <!-- 关注成功 -->
        <activity
            android:name=".explore.AttentionSuccessActivity"
            android:screenOrientation="portrait" />
        <!-- ===============================业务activity定义在这条线之上================================== -->
        <!-- 二维码 -->
        <activity
            android:name=".qrcode.CaptureActivity"
            android:screenOrientation="portrait" />
        <!-- shareSDK -->
        <activity
            android:name="com.mob.tools.MobUIShell"
            android:configChanges="keyboardHidden|orientation|screenSize"
            android:screenOrientation="portrait"
            android:theme="@android:style/Theme.Translucent.NoTitleBar"
            android:windowSoftInputMode="stateHidden|adjustResize" >
            <intent-filter tools:ignore="GoogleAppIndexingWarning" >

                <!-- <data android:scheme="tencent" /> -->

                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.BROWSABLE" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
        <!-- 微信分享回调 -->
        <activity
            android:name=".wxapi.WXEntryActivity"
            android:configChanges="keyboardHidden|orientation|screenSize"
            android:exported="true"
            android:screenOrientation="portrait"
            android:theme="@android:style/Theme.Translucent.NoTitleBar" />
        <activity android:name=".me.activities.MyActivitiesActivity" >
        </activity>
        <!-- 个人信息 -->
        <activity android:name=".me.information.PersonalInformationActivity" >
        </activity>
        <!-- 昵称 -->
        <activity android:name=".me.information.NicknameActivity" >
        </activity>
        <!-- 个人简历 -->
        <activity android:name=".me.information.ResumeActivity" >
        </activity>
        <!--活动详情-->
        <activity android:name=".home.activities.ActivitiesDetailActivity" >
        </activity>
    </application>

</manifest>
